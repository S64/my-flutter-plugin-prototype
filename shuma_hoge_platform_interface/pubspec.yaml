name: shuma_hoge_platform_interface
version: 1.0.0-SNAPSHOT
description: Shuma's prototype flutter plugin - common interface
homepage: https://gihub.com/S64

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
